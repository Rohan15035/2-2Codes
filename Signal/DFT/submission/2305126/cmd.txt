# CSE 220 -- Offline: DFT & FFT -- the exact commands that produced outputs/

# ---------------------------------------------------------------- Task A
python3 bigmul.py inputs/1.txt --engine dft --out-dir outputs/task_a/1
python3 bigmul.py inputs/2.txt --engine dft --out-dir outputs/task_a/2
python3 bigmul.py inputs/3.txt --engine fft --out-dir outputs/task_a/3
python3 bigmul.py inputs/4.txt --engine fft --out-dir outputs/task_a/4
python3 bigmul.py --benchmark --out-dir outputs/task_a/benchmark

# ---------------------------------------------------------------- Task B
python3 image_conv.py images/skyline512.png --kernel bokeh --param 9 \
    --engine fft --out-dir outputs/task_b/skyline_bokeh
python3 image_conv.py images/sunset512.png --gray --kernel motion --param 41 \
    --engine fft --out-dir outputs/task_b/sunset_motion
python3 image_conv.py images/skyline256.png --gray --kernel gaussian --param 21 \
    --engine dft --out-dir outputs/task_b/skyline256_gaussian_dft
python3 image_conv.py images/nebula512.png --kernel bokeh --param 13 \
    --engine fft --out-dir outputs/task_b/nebula_bokeh
python3 image_conv.py images/skyline512.png --benchmark \
    --out-dir outputs/task_b/benchmark

# ------------------------------------------------- Bonus: ArbitraryLengthFFT
# Bluestein chirp-z, so the transform runs at the exact linear-convolution
# size (N = 5, 99, 1023, 9999 instead of 8, 128, 1024, 16384; 530 x 530
# instead of 1024 x 1024) rather than padding up to a power of two.
python3 bigmul.py inputs/1.txt --engine arbitrary --out-dir outputs/bonus/task_a/1
python3 bigmul.py inputs/2.txt --engine arbitrary --out-dir outputs/bonus/task_a/2
python3 bigmul.py inputs/3.txt --engine arbitrary --out-dir outputs/bonus/task_a/3
python3 bigmul.py inputs/4.txt --engine arbitrary --out-dir outputs/bonus/task_a/4
python3 image_conv.py images/skyline512.png --kernel bokeh --param 9 \
    --engine arbitrary --out-dir outputs/bonus/task_b/skyline_bokeh_arbitrary
python3 image_conv.py images/skyline256.png --gray --kernel gaussian --param 21 \
    --engine arbitrary --out-dir outputs/bonus/task_b/skyline256_gaussian_arbitrary

# ------------------------------------------------------------ cross-checks
# Inputs 1 and 2 through every engine give byte-identical product.txt, and
# the optional schoolbook multiply agrees with all three transform engines.
python3 bigmul.py inputs/1.txt --engine schoolbook --out-dir outputs/bonus/task_a/1_schoolbook
python3 bigmul.py inputs/2.txt --engine schoolbook --out-dir outputs/bonus/task_a/2_schoolbook
